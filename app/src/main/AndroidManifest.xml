<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jomnucontractors">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JomnuContractors">
        <activity android:name=".About_us"></activity>
        <activity android:name=".Contact" />
        <activity android:name=".Server" />
        <activity android:name=".Android" />
        <activity android:name=".webdesign" />
        <activity android:name=".robotics" />
        <activity android:name=".archi" />
        <activity android:name=".civil" />
        <activity android:name=".Home" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>